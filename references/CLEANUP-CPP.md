# C++ Cleanup

- Baseline: repo build command, compiler warnings, `clang-tidy`, `include-what-you-use`.
- Cleanup signals: unused includes/symbols, include graph issues, duplicated helpers, legacy compile flags.
